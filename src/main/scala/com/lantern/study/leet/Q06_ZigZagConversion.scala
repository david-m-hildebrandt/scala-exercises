package com.lantern.study.leet

import scala.collection.mutable

object Q06_ZigZagConversion {

  object Solution {
    def convert(s: String, numRows: Int): String = {

      var rows = List((1 to numRows).map(List[Char]()))

      for {
        i <- 0 until s.length
      } {
//        val c = s.charAt(i)
 //       val list : List[Char] = List
  //      rows(i) = list.
      }



      ""
    }
  }

}
